[package]
name = "test5"
version = "0.1.0"
edition = "2024"

[dependencies]
# Excel出力専用のクレートを追加
rust_xlsxwriter = "0.94" 

# Excel読み込み専用のクレートを追加
calamine = "0.34" 

# jsonの読み込みクレート
# 部品表にどの項目を追加するかを決める
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"